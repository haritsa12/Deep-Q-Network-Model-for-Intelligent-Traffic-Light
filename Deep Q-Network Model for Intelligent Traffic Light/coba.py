import numpy as np
import math

# timings = np.random.weibull(2, 10)
# timings = np.sort(timings)

# generated_steps = []
# min_old = math.floor(timings[0])
# max_old = math.ceil(timings[-1])
# min_new = 0
# max_new = 5000

# for value in timings:
#     generated_steps = np.append(generated_steps, ((max_new - min_new) / (max_old - min_old)) * (value - max_old) + max_new)

# generated_steps = np.rint(generated_steps)  # round every value to int -> effective steps when a car will be generated

# print (generated_steps)


